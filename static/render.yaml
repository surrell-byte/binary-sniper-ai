services:
  - type: web
    name: binary-sniper-ai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn backend:app --host 0.0.0.0 --port 10000"
    plan: free